#include <stdio.h>
#include <stdlib.h>
#define SIZE 5

int main(void) {
    int index, numbers[SIZE];
    int sum = 0;
    float average;

    for (index = 0; index < SIZE; index++)
        sum += score[index];
    average = (float) sum / SIZE;

    printf

    return 0;
}